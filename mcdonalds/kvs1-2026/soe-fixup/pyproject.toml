[project]
name = "soe-fixup"
version = "0.2.0"
description = "Per-stage SOE conversion/cleanup payload generator (CyberArk PSM workflow)"
requires-python = ">=3.11"
dependencies = ["openpyxl", "pyperclip"]

[dependency-groups]
dev = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["tests"]
