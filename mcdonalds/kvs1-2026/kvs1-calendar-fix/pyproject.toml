[project]
name = "kvs1-calendar-fix"
version = "0.1.0"
description = "KVS1 conversion night: force the MyRestaurant close time into eBOS on the live SOE"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["tests"]
